{
	"name": "user-accounts",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "better-npm-run start",
		"dev": "better-npm-run dev",
		"prod": "better-npm-run prod",
		"test": "(dropdb --if-exists test-Asap && createdb test-Asap) && NODE_ENV=test & npx mocha"
	},
	"betterScripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": {
			"command": "node ./src/app.js",
			"env": {
				"DEBUG": "backend:*",
				"NODE_ENV": "development"
			}
		}
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"bcrypt": "^5.1.1",
		"better-npm-run": "^0.1.1",
		"debug": "^4.3.6",
		"dotenv": "^16.4.5",
		"express": "^4.19.2",
		"mongoose": "^8.5.2"
	}
}
